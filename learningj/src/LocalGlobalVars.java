public class LocalGlobalVars {
    public static void main(String[] args) {
        //global vars declared outside a method, but inside a class. visible to all parts of class
        DiceRoller diceRoller = new DiceRoller();
    }
}
